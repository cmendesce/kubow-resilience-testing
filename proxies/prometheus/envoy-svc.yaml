apiVersion: v1
kind: Service
metadata:
  name: prom-proxy
  labels:
    app: prom-proxy
  namespace: monitoring
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 8082
    targetPort: http
    protocol: TCP
  selector:
    app: prom-proxy