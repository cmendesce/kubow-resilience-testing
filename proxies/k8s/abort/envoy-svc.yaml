apiVersion: v1
kind: Service
metadata:
  name: k8s-proxy
  labels:
    app: k8s-proxy
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 8081
    targetPort: http
    protocol: TCP
  selector:
    app: k8s-proxy